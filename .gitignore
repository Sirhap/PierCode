# Binaries
piercode
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Dependency directories
vendor/
node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Extension build artifacts
extension/*.crx
extension/*.pem
extension/dist/

# Jekyll (GitHub Pages docs site) local build output
docs/_site/
docs/.jekyll-cache/
docs/.jekyll-metadata
.bundle/

# OpenLink/omx build and dev tools
.omx/
.claude/
.todos.json
.piercode/
.playwright-mcp/
release-packages/
release/
dist/

# Superpowers brainstorm scratch
.superpowers/

# Landing page build output
site/dist/
site/node_modules/

# ChatGPT reverse-engineering proxy — never commit (ToS + token logic)
chatgpt-proxy/

# PyInstaller-built proxy binaries (embedded at release build, not committed)
internal/subproc/proxybin/
chatgpt-proxy/.pyi-build/
chatgpt-proxy/build/
chatgpt-proxy/dist/
*.spec

# reverse-engineered reference porting specs (local only, not committed)
tmp/
